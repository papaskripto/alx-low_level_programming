#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string
 * @s: string to be evaluated
 *
 * Return: the length of a string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
